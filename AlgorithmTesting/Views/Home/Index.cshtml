@{
    ViewData["Title"] = "Home Page";
}
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.7.3/Chart.bundle.min.js"></script>

<h1>Results</h1>
<div class="chart-container" style="position: relative; margin: auto; height:50vh; width:80vw">
    <canvas id="myChart" width="400" height="400"></canvas>
</div>

<p>These results are an average from @ViewBag.timesRan iterations of the method being tested at each array size.</p>

<h2>Method 1</h2>
<table class="table table-striped" id="data">
  <tr>
      <th>Array Size</th>
      <th>Time (Nanoseconds)</th>
  </tr>
    @foreach (var item in ViewBag.result)
    {
        <tr>
            <td>@item.Key</td>
            <td>@item.Value</td>
        </tr>
    }
</table>

<h2>Method 2</h2>
<table class="table table-striped" id="data2">
    <tr>
        <th>Array Size</th>
        <th>Time (Nanoseconds)</th>
    </tr>
    @foreach (var item in ViewBag.result2)
    {
        <tr>
            <td>@item.Key</td>
            <td>@item.Value</td>
        </tr>
    }
</table>